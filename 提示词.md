
## 🎨 UI 设计提示词（中文）

### 核心风格
> 一个专业的多智能体 AI 聊天平台界面，采用**深色科技风（Dark Tech Theme）**，整体呈现**深海蓝 + 霓虹青绿**的配色方案，界面简洁、信息层级清晰，具有强烈的未来感与系统感。左侧为功能导航边栏，右侧为主对话区域，中间展示智能体协作的推理链条。

---

### 布局结构（Layout）

**整体布局：**
- **左侧边栏（Sidebar）**：宽度约 240-280px，固定定位，背景色为极深蓝（`#0B1120` 或 `#0F172A`），包含 Logo、新建会话按钮、功能菜单（知识库查询、服务站查询、联网搜索、历史会话）、底部状态提示。
- **右侧主区域（Main Content）**：flex 布局，背景色为稍浅的深蓝（`#111827` 或 `#1E293B`），顶部有用户头像区，中间为对话流（Chat Flow），底部为输入框。
- **对话流区域**：垂直排列，包含用户消息气泡、AI 思考过程（可折叠的推理链）、AI 正式回复内容。

---

### 配色方案（Color Palette）

| 元素 | 颜色值 | 说明 |
|------|--------|------|
| 侧边栏背景 | `#0B1120` | 极深蓝，几乎纯黑 |
| 主内容区背景 | `#111827` | 深蓝灰 |
| 卡片/气泡背景 | `#1E293B` | 用于 AI 回复卡片、思考过程区域 |
| 主强调色 | `#10B981` 或 `#34D399` | 青绿色，用于用户消息边框、状态指示点、智能体交接标识 |
| 次要强调色 | `#3B82F6` | 亮蓝色，用于"ITS" Logo、图标高亮 |
| 文字主色 | `#F1F5F9` | 浅灰白，主标题与正文 |
| 文字次色 | `#94A3B8` | 中灰，用于描述、时间戳、思考过程文本 |
| 边框/分割线 | `#1E293B` / `#334155` | 极细分割线，保持界面干净 |

---

### 组件细节（Component Details）

**1. 左侧边栏**
- **Logo 区域**：左上角显示 "ITS" 字样，使用蓝色（`#3B82F6`）圆角矩形徽标，字体为无衬线粗体。
- **新建会话按钮**：带有 `⌘+K` 快捷键提示，文字为浅灰，hover 时背景变亮。
- **菜单项**：图标 + 文字形式（如 📚 知识库查询、🔧 服务站查询、🌐 联网搜索、💬 历史会话），默认文字色 `#94A3B8`，选中态为白色 + 左侧青绿色竖线指示器。
- **历史会话区**：底部显示"暂无历史对话"，文字为灰色斜体或低透明度。

**2. 用户消息气泡（User Message Bubble）**
- 位于右侧，对齐方式 `align-self: flex-end`。
- 背景透明或半透明深色，**边框为 1px 青绿色（`#10B981`）实线**，圆角约 12px。
- 文字为白色，内边距适中（12-16px）。

**3. AI 思考过程 / 推理链（Reasoning Chain）**
- **可折叠面板**：顶部有"思考过程 ▼"字样，带下拉箭头。
- **智能体卡片**：背景为半透明深色卡片（`rgba(30, 41, 59, 0.6)`），左侧有**青绿色竖条**作为时间线/状态指示。
- **智能体标识**：显示"🤖 当前智能体: 主调度智能体"，字体为青绿色或白色粗体。
- **思考文本**：灰色小号字体（`#94A3B8`，14px），模拟代码/日志风格，左对齐。
- **智能体交接（Handoff）**：显示为"调度智能体 ➡ 咨询技术专家"，使用箭头图标和标签式（Tag）设计，背景为深蓝色，文字青绿色。
- **状态指示**：绿色圆点（`#10B981`）表示进行中/活跃状态。

**4. AI 正式回复（AI Response）**
- 白色标题文字，如"今天（2026年1月12日前后）发生的新鲜事包括："。
- 次级标题加粗，如"**国内要闻：**"。
- 内容使用无序列表（Bullet List），圆点为实心，行间距舒适。
- 整体无气泡边框，直接以文本流形式呈现，保持阅读沉浸感。

**5. 输入框（Input Area）**
- 位于底部，固定或粘性定位。
- 背景为深色卡片（`#1E293B`），圆角较大（约 16-20px）。
- 内部有占位符文字（Placeholder），颜色为灰色。
- 无边框或极细边框，focus 时有青绿色光晕（Glow Effect）。

---

### 字体与排版（Typography）

- **字体家族**：`Inter`、`SF Pro Display`、`PingFang SC`（中文）或 `Noto Sans SC`，无衬线。
- **标题**：16-18px，字重 600（Semi-Bold），白色。
- **正文**：14-15px，字重 400（Regular），行高 1.6-1.8。
- **思考过程文本**：13px，字重 400，灰色，行高 1.5，模拟等宽字体或标准无衬线。
- **标签/徽章**：12px，字重 500，青绿色或蓝色。

---

### 交互与动效（Interaction & Motion）

- **思考过程展开/收起**：平滑的高度过渡动画（`height: auto` transition 或 accordion 效果），箭头旋转 180°。
- **智能体交接**：带有轻微的滑入动画（Slide In），表示智能体切换。
- **消息进入**：新消息从底部淡入上浮（Fade In + Translate Y）。
- **Hover 效果**：菜单项、按钮 hover 时背景色从 `#0B1120` 过渡到 `#1E293B`，过渡时间 200ms。
- **输入框 Focus**：外发光（Box Shadow）`0 0 0 2px rgba(16, 185, 129, 0.3)`。

---

